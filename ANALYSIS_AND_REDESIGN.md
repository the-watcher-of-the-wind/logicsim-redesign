# LogicSim 业务分析与改造方案

## 项目与来源

- 原网站：<https://kuangdash.gitlab.io/logicsim/>
- 原源码：<https://gitlab.com/kuangdash/logicsim>
- 本地基线：GitLab `main` 分支，提交 `4498910e8f8efff0d7dfd4840739298e2a29f06e` 的源码归档。
- 技术形态：纯静态 HTML、CSS、JavaScript。JointJS 负责图形绘制，Dagre 负责自动布局；无需后端，也没有构建步骤。

原仓库没有提供许可证文件。项目权利人已确认可以复制、修改并公开发布；本项目保留来源与原提交标识，不代原作者添加许可证。

## 业务功能与使用流程

网站面向学习命题逻辑、数字电路或需要展示逻辑结构的用户。它将**逆波兰逻辑表达式**转换为由输入、常量、选择器和输出组成的图。它是逻辑结构可视化工具，当前并不提供可交互的电路信号仿真。

| 运算符 | 含义 | 示例 |
| --- | --- | --- |
| `.` | 与 | `a b .` |
| `,` | 或 | `a b ,` |
| `<` | 非 | `a <` |
| `>` | 蕴含 | `a b >` |
| `=` | 等价／同或 | `a b =` |

核心流程是：输入表达式 → 解析为逻辑决策结构 → 生成包含 `nodeArray` 与 `linkArray` 的 JSON → 使用 JointJS 绘图并自动排列。原页面要求用户依次点击“解析文本”和“文本转图”。

其他能力包括：直接编辑 JSON 并加载到画布、把当前图转回 JSON、导入／下载本地 JSON 文件、在画布中移动元素、通过小地图平移视图，以及缩放。页面提供节点名称和备注输入区，但原实现的保存逻辑沿用了不适用的 `Sheet` 数据结构，因此这项能力实际上不可靠。

### 数据格式

顶层结构为 `{ "nodeArray": [], "linkArray": [] }`。节点主要包含 `key`、`type`、`name`；连线包含 `from`、`frompid`、`to`、`topid`。新版仅增加可选的节点 `memo` 字符串，继续接受不带 `memo` 的原有文件。

## 当前问题

1. 首屏同时展示表达式、原始 JSON、画布和较长说明，主操作不突出；固定尺寸布局在窄屏下拥挤。
2. 两次点击才能从表达式得到图；页面初始 JSON 示例并非严格 JSON，首次加载时会被错误处理逻辑清空。
3. 解析器对空输入和缺少操作数有提示，但错误信息不一致，部分非法输入没有准确定位。JSON 解析失败时会清空当前图和编辑内容，容易造成数据丢失。
4. 节点名称／备注编辑代码与实际节点类型不匹配，导出逻辑也没有保留备注。
5. 导出的文件名和图中反馈不够明确；部分状态文字仍是英文。页面没有系统化的键盘与窄屏交互。

## 可行方向与本次选择

| 方向 | 具体做法 | 本次处理 |
| --- | --- | --- |
| 易用性与可靠性 | 合并主流程、校验输入和 JSON、修复节点编辑及导入导出 | 作为视觉改版所需的基础修复 |
| 视觉与布局 | 浅色工作台、突出画布、精简操作区、响应式排列、JSON 收入高级面板 | **本次重点** |
| 逻辑分析扩展 | 真值表、表达式等价检查、交互式输入信号 | 暂不加入，避免改变产品定位与项目规模 |

本次采用“清爽工作台”：顶部提供标题和文件操作；左侧负责表达式输入、示例与运算符说明；中间是主画布；右侧为节点信息；小地图保留。JSON 编辑区默认收起，但手工加载和图转文本入口仍可用。小屏幕上各区域改为纵向排列，并保证画布具备可操作的高度。

## 验收标准

- 五种运算符及 README 中的组合表达式均可一键生成图；空输入和操作数不足时给出中文提示，不改变当前图。
- 错误 JSON 不覆盖当前图或用户输入；原格式 JSON 与含可选 `memo` 的 JSON 都可导入、绘制、导出并再次导入。
- 选中节点后可修改名称和备注，修改在 JSON 和本地文件往返后仍存在。
- 文件操作、缩放、小地图和响应式布局可用；GitHub Pages 子路径下无资源加载错误。
